package com.example.android_nixie_v2

class Nap {
    val amount : Int
    val durationMin : Int

    constructor(amount : Int, durationMin : Int) {
        this.amount = amount
        this.durationMin = durationMin
    }
}