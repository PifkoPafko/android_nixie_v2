<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/bluetooth_connection"
        android:icon="@drawable/ic_bluetooth_disconnected"
        android:title=""
        android:contentDescription="@string/top_app_bar_edit_content_description"
        app:showAsAction="always"
        />

    <item
        android:id="@+id/add"
        android:icon="@drawable/ic_add_menu"
        android:title="@string/top_app_bar_add"
        android:contentDescription="@string/top_app_bar_add_search_content_description"
        app:showAsAction="always" />

</menu>